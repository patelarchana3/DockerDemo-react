sudo: required

services:
  - docker

before_install:
  - docker build -t patelarchana84/dockerdemo-react -f Dockerfile.dev .

script:
  - docker run patelarchana84/dockerdemo-react npm run test -- --coverage
